# v2.0 Realtime Public-State Architecture Plan

**Status:** RC.2 beta-default candidate deployed and validated on the isolated
beta stack; production cutover has not started

**Decision date:** August 31, 2026

**Release target:** `v2.0.0-rc.2` for production testing, followed by `v2.0.0`
after live validation. RC.1 remains the immutable opt-in source-proof release;
the default-source and operational-control changes advance the candidate to
RC.2.

**Current production architecture:** Unchanged; Neon plus adaptive polling
remains authoritative and supported

**Candidate architecture:** Neon-authoritative state with one Cloudflare
Durable Object public hub per agency

**Implementation record through September 1, 2026:** Phase 1/2 foundation, the
deployed Phase 3 shadow-publication slice, and the first deployed Phase 4
dual-read client slice are in progress on
`codex/v2-realtime-beta`. The
standalone, disabled-by-default Worker
proof defines the protocol-v1 public-state allowlist, canonical checksums,
monotonic revision handling, authenticated publication, hibernatable
subscribe-only WebSockets, and a reproducible local verification harness. The
separate `wth_apps/lotto-beta` Vercel project deploys only from that branch at
the stable `https://beta.williamtemple.app` origin; the generated
`https://lotto-beta-sigma.vercel.app` hostname remains an alias. Its isolated
Portland Neon resource, `neon-copper-queen`, has the complete canonical schema
and the public app plus `/api/state` pass initial smoke checks. The separate public
`lotto-beta-blob` store is connected in Portland. A sending-only, domain-scoped
`LOTTO Beta` Resend key is installed only in the beta Vercel Production
environment; its Magic Link request succeeded and Resend reported the test
message delivered. Opening that link established an authenticated beta
`/admin` session and reached Persistence confirmed. The key deliberately
remains in the existing Resend workspace because moving `williamtemple.app`
would also affect live LOTTO and the separately hosted FEED application;
account/domain migration is outside this proof and deferred to a coordinated
cutover. A DNS-only Cloudflare CNAME and Vercel ownership TXT record now bind
the stable beta origin without changing the apex, `www`, or `feed` records.
`AUTH_URL` and Auth.js callbacks use that origin, its Magic Link completed an
authenticated Admin session, and a generated-host session did not cross to it.
Beta-only crawler blocking and the staff/sign-in warning banner are implemented
behind the explicit `LOTTO_DEPLOYMENT_ENVIRONMENT=beta` setting. The standalone
Worker is deployed at `https://lotto-realtime-beta.et2-geiger.workers.dev`; its
rotated beta publish secret is stored only by Cloudflare and the isolated beta
Vercel project, preview URLs are disabled, and a remote synthetic
verification passes health, authentication, snapshot, WebSocket, idempotency,
monotonicity, and origin-policy checks. Phase 2 is not complete: repeat/stress
concurrency, hibernation/wake, network interruption, platform measurement, and
physical legacy-device tests remain. A first bounded fanout run delivered the target
revision to every one of 1, 10, 100, and 200 simultaneous clients, but repeated
and sustained-load evidence is still required. A bounded synthetic A/B runner
now compares equal polling and WebSocket cohorts against one isolated object,
records timestamped JSON evidence, and refuses non-beta remote targets. The
Phase 3 code now defines the additive Neon revision/outbox schema, atomically
allocates a revision and pending public projection from the central state-write
transaction, performs at most one bounded post-commit shadow publication, and
provides internal newest-only repair and payload-free status methods. The
additive schema is applied and verified on isolated beta Neon. A flag-off
deployment proved an authenticated write advanced the authoritative revision
without creating an outbox row. A second beta-only deployment enabled shadow
publication; one authenticated write produced matching Neon/outbox revision
`3`, status `accepted`, and the same checksum in the Durable Object snapshot.
The Worker is now connected to Neon only as a derived beta shadow destination;
no client surface trusts it, public polling is unchanged, and production plus
`main` remain unchanged. The Phase 4 implementation adds a beta-only server
flag plus explicit `?realtime=observe` browser cohort for Home, Display,
Inventory, and Arcade.
Those clients validate and compare the pushed projection but cannot render it;
the observer adds no fetch and stops after bounded reconnect attempts. The
first live browser mutation advanced the isolated stack to revision `16`,
arrived over WebSocket in `127 ms`, briefly showed the hub ahead of the
authoritative poll, and converged by checksum in `1,221 ms` with zero
reconnects. Repeated refreshes retained that first convergence measurement, and
the Admin diagnostic reported revision `16` accepted on its first attempt. The
expanded cohort then matched exact hub/poll revision `16` on all four surfaces;
the response header and a later browser observation agreed at revision `70`.
One Display observer remained connected and exactly matched for 307 seconds,
and Safari on the iOS 15.4 iPad mini 4 simulator returned to the same match
after a real app background/foreground cycle. A pre-existing UTC/Pacific
service-date hydration mismatch surfaced on both canary and control pages and
was fixed and revalidated on deployed beta: both pages rendered the Pacific
date without console errors, and the observer still matched exact revision
`70`. The
contracts live in
[`REALTIME_SHADOW_PUBLICATION.md`](./REALTIME_SHADOW_PUBLICATION.md) and
[`REALTIME_CLIENT_CANARY.md`](./REALTIME_CLIENT_CANARY.md).

**Current implementation status (September 3, 2026):** Phase 5 is built and
deployed on the isolated beta branch and infrastructure. Home, Display,
Inventory, and Arcade all support the rendered-source controller behind the
exact `?realtime=source` cohort; healthy source clients stop scheduled polling
and immediately restore the existing adaptive poller when source authority is
lost. All four surfaces passed a live beta revision transition, a healthy
40-second Display window made zero `/api/state` requests, tab-local network
loss and recovery selected the expected fallback path, and the iOS 15.4
simulator passed connection, background/foreground, and subsequent-update
checks. RC.1 remains the immutable opt-in source-proof checkpoint. RC.2 now
makes realtime the deployed default on the four ordinary beta URLs, adds an
explicit polling-only override, and retains automatic fallback.
Production-domain and account changes are not implemented; production and
`main` remain unchanged. The Phase 6 safety slice is deployed: one server-only Vercel
application gate overrides rendered realtime for new page loads, a separately
authenticated Durable Object control can durably drain and later resume an
agency's WebSocket connections, and pushed revisions now update the same
activity clock used by adaptive polling. Vercel deployment
`48MNHWZZ2Qg9yjdCBesNASrp3nPx` serves commit `df180f0` at the stable beta
origin. Ordinary Home, Display, Inventory, and Arcade URLs all connected at
revision `91`; the polling-only control opened no source socket, the observer
reported an exact Neon match, a live Worker drain forced a fresh Display load
to `Polling fallback · connection closed`, and resume restored the same page
to `Realtime source · live · r91`. The parameter-free Display also hydrated
and connected on the iOS 15.4 iPad mini 4 simulator. The Vercel application
gate is covered by configuration, mount, full-suite, and production-build
tests but has not been toggled on the hosted beta because that operational
exercise requires a disable deployment followed by a restore deployment.

## Decision statement

LOTTO will investigate a Cloudflare Durable Object realtime hub as the
provisional architectural direction for v2.0. The hub would retain the latest
allowlisted public queue projection and broadcast versioned state to connected
public clients over hibernatable WebSockets. Neon would remain authoritative
for authenticated commands, atomic state changes, undo/restore snapshots,
authentication, configuration, translations, and administrative reporting.

This is not yet a final architecture decision. A major-version release is
appropriate only after the candidate proves that it improves public freshness
and Neon scale-to-zero behavior without weakening state integrity, recovery,
security, legacy-device compatibility, or the current app's ability to operate
when the hub is unavailable.

Until the final v2.0 gate passes:

- the current `/api/state` read/write behavior remains the production baseline;
- adaptive polling remains implemented, tested, and deployable;
- no production staff command may depend on Cloudflare availability;
- all database changes must be additive and backward-compatible;
- realtime activation must be independently reversible by environment and
  client cohort; and
- a failed experiment returns the project to the current architecture without
  data migration or emergency code removal.

## Why this warrants v2.0

The change introduces a second runtime, a public event-delivery protocol,
cross-system publication, version reconciliation, realtime connection state,
and new operational failure modes. Even though the existing user workflows can
remain visually familiar, the way public state reaches every Home, Display,
Inventory, and Arcade surface changes fundamentally.

The major version is reserved for the proven architecture, not for the proof of
concept. Experimental and shadow-mode work may land during v1.x while disabled
by default. v2.0 is not cut merely because the prototype connects successfully.

## Planned v2.0.0-rc.2 validation and cutover

The release candidate will keep `https://beta.williamtemple.app` as its stable
validation origin. Unlike Phase 5's opt-in cohort, the RC will make the
realtime/fallback controller the default for every ordinary public URL:

| Surface | RC default URL |
| --- | --- |
| Home | `https://beta.williamtemple.app/` |
| Display | `https://beta.williamtemple.app/display` |
| Inventory | `https://beta.williamtemple.app/inventory` |
| Arcade queue banner | `https://beta.williamtemple.app/arcade` |

Each URL will still begin with an authoritative Neon-backed state read and
exact revision/checksum handshake. Once the hub matches, realtime becomes the
rendered source and scheduled polling stops. Invalid data, a revision gap,
connection loss, timeout, background return, or a disabled runtime capability
immediately selects the existing adaptive polling source.

The RC implementation must also retain these validation controls:

- `?realtime=poll` forces the existing polling-only path for a side-by-side
  control and per-device rollback;
- `?realtime=observe` retains the non-rendering dual-read diagnostic when it is
  still useful during validation;
- `?realtime=source` remains accepted as a compatibility alias during RC.2 but
  is no longer required; and
- a server-only Vercel switch prevents realtime application on newly loaded
  clients after redeployment; and
- a separately authenticated Cloudflare control durably refuses new sockets
  and closes already-connected sockets, which makes loaded clients reconcile
  once with Neon and resume adaptive polling.

Authenticated staff workflows remain Neon-authoritative and do not use the
public WebSocket as their data source. Staff actions continue to commit even if
Cloudflare is unavailable.

### Production test cutover

**Owner decision — September 2, 2026:** William Temple House v2.0 will be
deployed as a new project in the consulting business's existing paid Vercel Pro
account. Cloudflare will then route the original `williamtemple.app` domain to
that verified project. The earliest preferred change window is Thursday,
September 3, 2026 after 2:30 PM America/Los_Angeles, following the pantry's last
service period of the week. This records the window; it does not authorize an
automatic or unattended cutover.

The validated RC commit is the promotion unit, not the isolated beta data or
secrets. `williamtemple.app` must **not** be pointed at the existing beta project
while that project is connected to `neon-copper-queen`, beta auth tables, beta
Blob storage, and the beta Durable Object namespace. Before the apex cutover:

1. Deploy the exact accepted release-candidate commit to a deliberately
   prepared successor project connected to production Neon,
   production Blob, production Resend/auth secrets, and a separate production
   Worker/Durable Object namespace.
2. Apply and verify only the additive revision/outbox schema in production.
3. Install production-scoped publication credentials, CSP origin, Worker origin
   allowlist, and runtime flags without copying beta rows or secrets.
4. Verify the production deployment through its generated Vercel hostname,
   including authentication, one harmless staff mutation, all four ordinary
   public URLs, polling fallback, and rollback.
5. Move the `williamtemple.app` domain assignment to that verified RC deployment
   and update `AUTH_URL`/callback configuration as one coordinated cutover.

The previous production deployment remains the immediate Vercel rollback
target. Disabling realtime returns the RC to polling without reverting the
schema; reassigning the domain returns the complete site to the previous build
if the RC itself is at fault.

Do not shut down the Hobby beta account/project or the previous production
project until the Pro deployment is serving the apex, authentication and all four
public surfaces pass, automatic fallback and both kill switches pass, provider
signals remain acceptable through the agreed validation checkpoint, and the
owner explicitly accepts the migration. After that acceptance, retire both
superseded deployments and their beta-only resources deliberately; do not
delete the rollback targets during DNS cutover.

### Realtime kill-switch contract

The two kill switches solve different timing problems without introducing a
new configuration polling loop:

1. `LOTTO_REALTIME_APPLICATION_ENABLED=false` overrides all rendered realtime
   configuration for newly loaded pages, including the non-rendering observer.
   Because Vercel environment changes
   apply to a new deployment, the operator changes the server-only value and
   redeploys the same known-good commit. It is never exposed through a
   `NEXT_PUBLIC_` variable.
2. `POST /v1/agencies/{agencyId}/control` with a Cloudflare-only
   `CONTROL_TOKEN` and `{ "mode": "drain" }` stores the drain state in the
   agency Durable Object, closes its current hibernatable sockets, and rejects
   new event connections with HTTP 503. Publishing and latest-state storage
   continue while drained, so staff writes and hub convergence are preserved.
   `{ "mode": "resume" }` reopens connections and new subscribers receive the
   newest stored projection.

The control credential is distinct from the publication credential and is not
installed in Vercel. An operator invokes it from a trusted administrative
environment through `npm run realtime:control`; every remote invocation
requires an exact mode, agency, and hostname confirmation string. The control
does not delete hub state, alter Neon, or stop staff commands.

When a socket is drained or otherwise fails, each visible client performs one
immediate authoritative `/api/state` reconciliation. The pushed and polled
paths share one last-observed-state clock, so the next successful poll uses the
existing burst, open/pre-open clamp, long-idle, off-hours, visibility, and error
rules. WebSocket reconnection retains its separate bounded jittered backoff and
cannot reset the polling activity clock. This behavior requires request-count
tests across all four public consumers.

### Scaled validation decision

For the current single-pantry deployment, the project owner has elected to
defer the ten-service-day beta observation, provider-wide fault injection, and
physical-iPad exercise. They are no longer prerequisites for building RC.2 or
beginning a controlled production test. Automated coverage, production-bundle
verification, the iOS 15.4 simulator, the beta four-surface proof, automatic
fallback, a verified rollback target, and active launch monitoring remain RC.2
requirements. Deferred evidence should be reconsidered before declaring the
stable `v2.0.0` release.

## Goals

1. Reflect a committed staff state change on connected public clients in
   near-real time.
2. Eliminate ordinary `/api/state` polling while a realtime connection is
   healthy.
3. Prevent public traffic from waking Neon or extending its active-compute
   window.
4. Preserve Neon transactionality, snapshots, undo/redo, and state recall.
5. Keep public state an explicit allowlisted projection, never a serialized
   copy of the complete internal `RaffleState`.
6. Make every cross-system operation idempotent, versioned, observable, and
   recoverable.
7. Preserve the current application as the automatic fallback throughout
   development and after release.
8. Keep incremental infrastructure cost within a documented nonprofit budget
   and measure it under realistic always-on display usage.

## Non-goals for the initial v2.0 architecture

- Moving authentication, translations, brand configuration, snapshots, or
  reporting out of Neon.
- Making Durable Object storage the authoritative queue database.
- Replacing the current Admin workflow or changing queue semantics.
- Using Neon logical replication, `LISTEN`/`NOTIFY`, or another long-lived
  database subscriber; those approaches can prevent Neon from scaling to zero.
- Removing polling before the realtime path has completed production canary
  validation.
- Guaranteeing literal zero infrastructure cost. Free allowances have hard
  limits and do not provide the same reliability guarantees as paid service.
- Adding gameplay traffic or Arcade state to the queue-state hub.

Promoting the Durable Object itself to authoritative operational storage is a
possible post-v2 investigation. It is explicitly outside this plan because it
would move transaction and recovery ownership away from the proven Neon state
manager.

## Current and candidate flows

### Current production flow

```text
public client ──poll──> Vercel /api/state ──query──> Neon

staff action ─────────> Vercel /api/state ──transaction──> Neon
```

### Candidate v2.0 flow

```text
                                  ┌──versioned state──> Home
                                  ├──versioned state──> Display
staff action                      ├──versioned state──> Inventory
    │                             └──versioned state──> Arcade banner
    ▼                                      ▲
Vercel authenticated command               │ WebSocket
    │                                      │
    ▼                                      │
Neon transaction ──after commit──> Cloudflare Worker
  current state                      │
  snapshot/history                   ▼
  monotonic revision             Durable Object
  publication outbox             latest public state
                                  connected clients

If realtime is unavailable: client immediately resumes the current bounded
polling strategy; staff commands continue to commit to Neon.
```

## Architectural invariants

These rules apply in every experimental phase and are release blockers if
violated.

1. **Neon commits first.** Public state is never broadcast before the
   authoritative transaction commits.
2. **One authority.** During v2.0, Neon decides the current queue state. The hub
   is a derived read model.
3. **Staff writes survive hub failure.** A Cloudflare timeout or outage cannot
   roll back or reject a state change that Neon committed successfully.
4. **Monotonic public revisions.** A client or hub never replaces a newer
   revision with an older one.
5. **Idempotent publication.** Repeating a publication produces the same final
   state and does not create a second logical transition.
6. **Explicit public schema.** Internal evidence such as `queueSession`, auth
   data, snapshot metadata, translation credentials, and Admin-only fields
   never enter the hub.
7. **Subscribe-only public clients.** Browsers cannot publish authoritative
   queue events.
8. **No unbounded background loops.** The Worker, Durable Object, and client do
   not use fixed perpetual timers to simulate reliability.
9. **Fallback remains bounded.** Connection failure can activate the existing
   adaptive polling strategy, including visibility pause, operating-hour
   cadence, error backoff, and request-budget tests.
10. **Protocol compatibility is explicit.** Client and hub exchange a
    `protocolVersion`; incompatible clients fall back instead of guessing.

## Candidate system boundaries

### LOTTO Next.js application on Vercel

Responsibilities:

- authenticate and authorize staff commands;
- execute the authoritative Neon transaction;
- increment the authoritative state revision;
- write publication intent in the same transaction;
- serialize the allowlisted public projection;
- sign and send a post-commit publication request;
- expose publication health to Admin without making it a prerequisite for a
  successful queue action; and
- retain the current read endpoint and polling behavior as fallback.

### Neon Postgres

Responsibilities:

- authoritative current queue state;
- atomic snapshots, undo/redo, and restore history;
- monotonic revision allocation;
- publication/outbox evidence;
- authentication and configuration stores; and
- authoritative recovery if the public hub is lost or rebuilt.

The implemented additive candidate gives the singleton `raffle_state` row an
authoritative `BIGINT` revision and records publication ID, revision, protocol
version, checksum, allowlisted payload, attempt state, timestamps, and a bounded
error summary in `raffle_public_state_publications`. It remains inert while the
shadow-publication flag is off.

### Cloudflare Worker and Durable Object

Responsibilities:

- route an agency's requests to one Durable Object using a stable opaque agency
  identifier;
- authenticate server-to-server publication;
- reject malformed, oversized, unsigned, replayed, or out-of-order updates;
- store only the newest valid public projection and protocol metadata;
- accept public read-only WebSocket connections;
- send the current projection on connection;
- broadcast a committed newer revision to connected clients;
- use the WebSocket Hibernation API so idle connections do not require active
  JavaScript duration; and
- emit operational metrics without visitor identity or ticket-search data.

There must be one object per agency, not one global singleton for every LOTTO
deployment. Development, preview, and production use distinct Worker
environments/namespaces so experimental state cannot cross boundaries.

### Public clients

Responsibilities:

- connect with the native browser WebSocket API;
- subscribe before resolving initial state so updates cannot fall into a
  fetch/subscribe race;
- compare every message revision with local state;
- ignore duplicates and older revisions;
- apply a full newer public projection atomically;
- fetch/resume fallback state on an invalid message, revision gap, unsupported
  protocol, or connection failure;
- expose a restrained connection status (`Live`, `Reconnecting`, or
  `Fallback`) where operationally useful; and
- stop ordinary state polling only after realtime has been declared healthy.

The initial implementation should broadcast the complete bounded public
projection rather than client-side deltas. Full state is easier to reason about,
idempotent, and recoverable. Delta messages should be considered only if
measured payload size or fanout cost requires them.

## Candidate protocol

The protocol is intentionally small. Exact names can change during the contract
phase, but these semantics must remain.

| Field             | Purpose                                                      |
| ----------------- | ------------------------------------------------------------ |
| `protocolVersion` | Reject incompatible client/hub behavior safely               |
| `agencyId`        | Route and validate tenant identity without exposing a secret |
| `publicationId`   | Idempotency and trace correlation                            |
| `revision`        | Monotonic authoritative state version                        |
| `committedAt`     | Authoritative Neon commit time                               |
| `publishedAt`     | Hub acceptance time for latency measurement                  |
| `checksum`        | Detect equal-revision payload conflicts or corruption        |
| `state`           | Allowlisted `PublicRaffleState` projection                   |

Candidate message types:

- `state`: current complete projection, sent on connection and after updates;
- `resync-required`: hub cannot safely establish continuity;
- `status`: optional protocol/service notice that carries no queue mutation; and
- `error`: bounded non-sensitive protocol failure information.

Revision handling at the hub:

- incoming revision greater than stored: validate, store, then broadcast;
- equal revision and equal checksum: return idempotent success;
- equal revision and different checksum: reject and alert as an integrity
  conflict;
- lower revision: do not store or broadcast; return the hub's current revision;
  and
- no stored revision: accept only an authenticated publication or report empty
  state to public clients so they enter fallback.

## Publication and recovery model

Postgres and Durable Object storage cannot share a transaction. The design uses
transactional intent plus idempotent delivery instead of claiming distributed
atomicity.

1. The authenticated action validates an optional command idempotency key and
   expected revision.
2. The Neon transaction applies the state change, writes its snapshot/history,
   increments revision, and records publication intent.
3. After commit, the Vercel handler attempts to publish the new public
   projection synchronously within a strict timeout.
4. Success records the accepted hub revision.
5. Failure leaves the authoritative action committed and the publication
   pending. Admin receives a non-destructive “public sync delayed” status.
6. Bounded retry uses the same publication ID and revision. Retryable failures
   use exponential backoff; overload signals do not trigger a retry storm.
7. A later staff action, an explicit Admin repair action, or an approved
   event-driven delivery mechanism drains only the newest necessary pending
   state. A fixed cron that repeatedly wakes Neon is not an acceptable repair
   strategy.
8. The hub's revision is periodically compared with the authoritative revision
   through staff/admin activity and deployment health checks, not visitor
   polling.

The prototype must compare at least three delivery choices before hardening:

- direct bounded post-commit publish plus outbox repair;
- Cloudflare Queue handoff after commit, with the Neon outbox retaining the
  pre-enqueue failure evidence; and
- Vercel-supported deferred work only if its retry/durability semantics meet the
  same requirements.

No option removes the final network gap after a database commit. The acceptance
criterion is visible, idempotent convergence—not an impossible exactly-once
claim.

## Security model

Public queue state is intentionally readable, but its integrity remains
security-sensitive.

- Use `wss://` only.
- The public WebSocket endpoint accepts subscriptions but no state mutation.
- The isolated beta proof currently authenticates publication with one
  least-privilege bearer secret, an exact beta-host allowlist, and a beta-only
  runtime gate. Rotate that secret before integrated testing.
- Before any production shadow canary, decide whether to replace the proof
  bearer with a server-to-server signature over method, path, agency ID,
  timestamp, nonce, and body digest.
- If signatures are adopted, enforce a short replay window, reject reused
  nonces, and support signing-key IDs with overlapping rotation.
- Store signing secrets only in Vercel and Cloudflare secret stores, never in a
  `NEXT_PUBLIC_*` variable or browser bundle.
- Validate the public-state schema and a conservative serialized payload limit
  before storage or fanout.
- Apply per-IP connection/rate controls and a per-agency connection ceiling.
- Ignore or close unexpected client frames rather than echoing them.
- Do not log ticket searches, session identifiers, email addresses, auth tokens,
  full state payloads, signatures, or secret material.
- Treat `Origin` checking as abuse reduction, not authentication; non-browser
  clients can forge it.

Threat-model and security review are required before the first production
canary.

## Feature flags and rollback controls

Activation must be decomposed so publishing, connecting, and stopping polling
are never one irreversible switch.

| Capability                    | Default before proof | Purpose                                            |
| ----------------------------- | -------------------- | -------------------------------------------------- |
| Hub service deployed          | Development only     | Exercise Worker/DO independently                   |
| Server publication            | Off                  | Shadow-copy committed public state                 |
| Client WebSocket connection   | Off                  | Observe connection behavior without changing reads |
| Client applies realtime state | Off                  | Compare pushed versus polled revisions             |
| Healthy client stops polling  | Off                  | Realize the cost reduction only after canary proof |
| Connection-status UI          | Internal/canary only | Diagnose without changing general workflow         |

A runtime capability response should be preferred over relying exclusively on
build-time `NEXT_PUBLIC_*` flags. A loaded client must automatically resume
polling if its connection fails even when a kill-switch deployment has not yet
reached that browser.

Rollback sequence:

1. Disable realtime application/connection for the affected cohort.
2. Clients resume the existing polling strategy immediately.
3. Disable server publication if the hub or protocol is the incident source.
4. Leave additive revision/outbox fields in place; they do not affect current
   state reads or writes.
5. Investigate from version/health metadata without deleting authoritative data.

## Production-shaped beta environment

The preferred integration and canary environment is a separate deployment at
`https://beta.williamtemple.app`, not a preview attached to the live WTH Vercel
project. This directly addresses the current production project's Preview
database-variable gap and provides a stable origin for WebSocket, authentication,
PWA, CSP, DNS, and legacy-device testing.

The beta hostname is only the visible entry point. Production-like testing
requires isolation at every stateful boundary:

| Layer          | Production                                 | Beta requirement                                                                                              |
| -------------- | ------------------------------------------ | ------------------------------------------------------------------------------------------------------------- |
| App            | WTH Vercel project at `williamtemple.app`  | Separate Vercel project at `beta.williamtemple.app`                                                           |
| Git promotion  | Production branch                          | Explicit beta/staging branch selected during provisioning                                                     |
| Database       | WTH production Neon database               | Fresh beta Neon database with schema plus synthetic/sanitized data                                            |
| Blob assets    | Production Appearance store                | Separate beta Blob store/token                                                                                |
| Authentication | Production secret, tokens, and auth tables | Separate secret and auth tables; host-only beta cookies                                                       |
| Email          | Production Resend configuration            | Separate beta API key/configuration; beta callback URL and clearly labeled messages                           |
| Realtime       | Not deployed initially                     | Deployed `lotto-realtime-beta` Worker and separate SQLite-backed Durable Object namespace                      |
| Hub origin     | Future production endpoint                 | `https://lotto-realtime-beta.et2-geiger.workers.dev`; custom beta realtime DNS remains optional and unprovisioned |
| Metrics/logs   | Production projects                        | Separate beta Vercel, Neon, and Cloudflare views                                                              |

The beta database must not be the production database. A fresh database is
preferred over an unsanitized branch because queue snapshots, authentication
records, integration credentials, translation API configuration, and other
operational data must not be copied into an experiment by default. Apply the
canonical `schema.sql`, seed synthetic queue history, and recreate only the
Appearance/configuration necessary for representative testing.

The beta deployment must also:

- use `AUTH_URL=https://beta.williamtemple.app` and a distinct `AUTH_SECRET`;
- verify that auth/session cookies remain host-only and do not cross between the
  apex and beta origins;
- keep beta Magic Link and OTP callbacks on the beta origin;
- use a separate Blob store so Appearance experiments cannot overwrite live
  assets;
- allow the beta app origin—and only that origin—on the beta hub's WebSocket
  abuse-control allowlist;
- add the beta WebSocket host to the beta CSP `connect-src` without broadening
  production CSP early;
- prevent indexing with a beta-specific `X-Robots-Tag`/robots policy;
- display an unmistakable non-production banner on staff/admin surfaces;
- prohibit production FEED credentials or irreversible external integrations;
- use independent spend alerts and the lowest practical Cloudflare limits; and
- avoid placing the Vercel app behind a Cloudflare proxy unless proxying the
  application itself becomes an explicit experiment. The app and realtime hub
  are separate origins and can be tested that way.

Promotion moves reviewed code, protocol versions, schema migrations, Worker
configuration, and runbooks. It never promotes beta queue data, auth rows, Blob
objects, Durable Object storage, or secrets into production.

Suggested environment progression:

1. local Next.js plus local `wrangler dev`;
2. Vercel beta project plus remote beta Worker/DO and beta Neon;
3. stable `beta.williamtemple.app` opt-in canary and simulator validation;
4. `v2.0.0-rc.2` on the same beta origin with realtime enabled by default for
   ordinary Home, Display, Inventory, and Arcade URLs;
5. the exact RC commit deployed behind a generated Vercel hostname with
   production-scoped Neon, Blob, auth/email, and Worker/Durable Object
   resources;
6. coordinated `williamtemple.app` assignment to that verified production RC;
   and
7. stable `v2.0.0` only after the production test is accepted.

## Measurement model

The experiment needs an explicit baseline and comparable post-change window.

### User-visible service objectives

Candidate promotion targets:

- p50 Neon-commit-to-client-render latency at or below 500 ms on a stable local
  network;
- p95 at or below 2 seconds and p99 at or below 5 seconds;
- reconnect and latest-state reconciliation within 5 seconds after a usable
  network returns;
- no visible regression to an older revision; and
- a clear fallback state instead of a frozen apparently-live display.

These are experiment targets, not promises. The baseline study may refine them
before production canary.

### Correctness objectives

- 100% agreement between Neon and the hub's newest revision/checksum at every
  completed validation checkpoint;
- zero public broadcasts for rolled-back/failed Neon transactions;
- duplicate and reordered deliveries converge to the newest state;
- every injected publication failure is visible and recoverable;
- undo, redo, restore, reset, and all other mutation types publish a new
  monotonic revision; and
- hub loss/recreation can be seeded safely from Neon.

### Resource objectives

- zero `/api/state` requests from a client during a one-hour healthy realtime
  test;
- at least 95% fewer public-origin state reads during the production canary;
- materially longer Neon suspended intervals outside authenticated work;
- no short repeating Durable Object alarms, timers, or application heartbeats
  that defeat hibernation;
- current-load Cloudflare usage remains inside the approved trial allowance;
  and
- a documented projected cost at 1×, 10×, and 100× current connected-client and
  state-change volume.

Free-tier success is not the production cost criterion. Workers Free fails
operations when a hard limit is exceeded. The final recommendation must include
the paid-plan floor, spend alerts, and the fallback behavior when a limit is
reached.

## Vercel usage impact of the realtime default

This assessment was refreshed from Vercel's official documentation on
September 2, 2026. The expected result is a net reduction in Vercel usage while
realtime is healthy, not a transfer of the removed Neon polling workload into
Vercel.

The public WebSocket connects from the browser directly to the Cloudflare
Worker hostname. It is not hosted by a Vercel Function and does not pass through
the Vercel deployment after the page has loaded. WebSocket connection duration,
frames, and fanout therefore belong to the Cloudflare usage envelope. Vercel
continues to serve the page, static assets, the initial `/api/state` handshake,
staff API actions, and any fallback reads.

| Vercel meter | Effect of the planned RC default |
| --- | --- |
| Edge Requests | Decrease because every scheduled `/api/state` poll removed is also one request no longer processed by Vercel's CDN. Page navigation, assets, initial state, and fallback still count. |
| Function invocations | Decrease because each removed dynamic `/api/state` request no longer invokes the state Function. Existing staff writes remain; publishing to Cloudflare occurs inside that already-required write invocation. |
| Active CPU | Expected to decrease with fewer state reads. Checksum/serialization and publication handling add bounded CPU only when staff changes state. Fluid Compute does not count time waiting on Neon or Cloudflare as active CPU. |
| Provisioned memory | Expected to decrease with fewer public read requests. A staff write can retain its Function memory slightly longer while the bounded Cloudflare publication is in flight, even though waiting is not active CPU. |
| Fast Origin Transfer | Decrease with the removed Function request/response traffic. Initial and fallback state responses still transfer through Vercel Compute. |
| Fast Data Transfer | Page and asset delivery remain. Realtime state frames use the Cloudflare connection instead of Vercel transfer; removed poll responses reduce Vercel transfer. |
| Builds and deployment storage | RC development creates ordinary build/storage usage but the realtime runtime itself adds no repeating build work. |

Vercel explicitly identifies excessive polling as an Edge Request optimization
target. Its current
[CDN pricing and usage guide](https://vercel.com/docs/manage-cdn-usage) lists
these monthly Hobby inclusions: one million Edge Requests, 100 GB Fast Data
Transfer, and 10 GB Fast Origin Transfer. The same guide lists ten million Edge
Requests in Pro's included allocation. Current
[Fluid Compute pricing](https://vercel.com/docs/functions/usage-and-pricing)
lists one million Function invocations, four Active CPU hours, and 360 GB-hours
of provisioned memory for Hobby.

Those allowances do not make Hobby an appropriate production plan. Vercel's
[Fair Use Guidelines](https://vercel.com/docs/limits/fair-use-guidelines)
restrict Hobby teams to non-commercial personal use and explicitly include work
performed for payment by an employee or consultant in their commercial-use
definition. Vercel also states that a Hobby team can be paused after exceeding
its included usage, whereas Pro supports on-demand usage and configurable spend
controls. The owner has confirmed that the consulting business already has a
paid Vercel Pro account and will own the new v2.0 production project there. The
temporary Hobby project remains a beta experiment and will be retired only
after the Pro deployment is accepted and its rollback checkpoint closes.

### Vercel trade-offs and failure envelope

- Every page load still makes the initial authoritative state read. The RC is
  not a zero-Vercel or zero-Neon design.
- Cloudflare publication adds a small amount of CPU, memory duration, and
  outbound work to an existing staff-write Function invocation; it does not add
  one new Vercel invocation per connected client.
- If many clients lose realtime together, each visible client can make one
  immediate fallback read and resume the existing adaptive polling cadence.
  That temporarily returns Vercel and Neon toward the current usage model and
  can create a short reconnect/fallback burst; it does not create an unbounded
  new loop.
- Ordinary page refreshes, route navigation, images, fonts, and other APIs still
  consume Vercel Edge Requests and transfer exactly as before.
- Directional savings are well supported by the request topology, but their
  magnitude must be read from the Vercel Usage dashboard rather than estimated
  from connected WebSocket counts.

### Lean launch monitoring

The single-pantry production test does not require a new monitoring platform.
Before cutover, capture the live project's current Vercel Usage counters. During
the first service day, keep the following provider views and application signal
available:

- Vercel Usage: Edge Requests, Function invocations, Active CPU, provisioned
  memory, Fast Origin Transfer, and Fast Data Transfer, filtered to LOTTO;
- Vercel Logs/Observability: Function errors, timeouts, and throttles;
- Cloudflare: Worker/Durable Object requests, errors, WebSocket activity,
  duration, and storage;
- Neon: active/suspended periods and compute usage; and
- LOTTO Admin realtime diagnostics: newest authoritative/publication revision,
  checksum agreement, attempt count, and failure state.

At launch, verify one harmless staff mutation on Home, Display, Inventory, and
Arcade. Treat unexplained revision disagreement, a failed newest publication,
repeated fallback across healthy clients, elevated Function errors, or an
unexpected usage surge as a rollback signal. On Pro, configure Vercel Spend
Management notifications before the cutover; Vercel documents web/email alerts
at 50%, 75%, and 100% of the chosen spend amount. Monitoring must not record
ticket searches, email addresses, auth tokens, or public-state payloads.

## Multi-phase delivery plan

Every phase ends with a written result and explicit go/no-go decision. Passing a
phase authorizes only the next phase, not the complete architecture.

### Phase 0 — Baseline, requirements, and experiment charter

**Objective:** establish what the candidate must beat and freeze the current
behavior as a protected fallback.

**Work:**

- Gather 7–14 representative days of Neon CU-hours, active/suspended windows,
  storage/history, and public network use.
- Record Vercel `/api/state` invocations, Edge Requests, transfer, response size,
  and latency by open/closed period.
- Measure connected/visible client counts and state changes per service day.
- Inventory every public state consumer and every state mutation path.
- Define `PublicRaffleState` fields and an initial size budget.
- Record current polling request-count tests and fallback behavior.
- Set the trial budget and Cloudflare account/ownership model.
- Decide the initial latency, convergence, uptime, and canary-duration targets.

**Exit gate:** baseline dataset, approved experiment budget, public-field
inventory, measurable success criteria, and an explicit statement that current
production behavior is unchanged.

**Rollback:** none required; this phase is read-only documentation and
measurement.

### Phase 1 — Protocol and local state-machine experiment

**Objective:** prove revision and reconciliation semantics without Cloudflare or
production data.

**Work:**

- Build a framework-neutral public projection serializer and schema.
- Model publication IDs, monotonic revisions, checksums, duplicate delivery,
  reordered delivery, revision gaps, and unsupported protocols.
- Build a small in-memory/local hub and a test client using the native WebSocket
  contract or a transport-neutral simulator.
- Determine whether the shared protocol belongs in a small workspace package or
  in isolated LOTTO/Worker modules with generated schemas and contract tests.
- Establish serialized-size limits using realistic maximum queue states.

**Required tests:** serializer allowlist, protected-field exclusion, payload
limit, idempotency, out-of-order rejection, equal-revision conflict, gap
recovery, full-state atomic application, and protocol-version fallback.

**Exit gate:** deterministic contract tests prove that arbitrary duplicate and
reordered messages cannot regress client or hub state.

**Rollback:** remove or retain the isolated experiment; no production path or
schema has changed.

### Phase 2 — Standalone Cloudflare proof of concept

**Objective:** prove the platform characteristics with synthetic state and no
LOTTO integration.

**Work:**

- Create a development-only Worker and SQLite-backed Durable Object.
- Use one object per synthetic agency and the Hibernation WebSocket API.
- Store a bounded latest projection and send it immediately on connection.
- Add a development publication endpoint with production-shaped signature and
  replay checks.
- Exercise local `wrangler dev` behavior and a remote development deployment.
- Measure connect, first-state, broadcast, hibernation/wake, and reconnect
  behavior.
- Evaluate Cloudflare's automatic ping/pong or WebSocket auto-response support
  before adding any application heartbeat.
- Enable development logs/metrics with payload redaction.

**Required tests:** 1/10/100/200 concurrent synthetic clients, rapid updates,
idle hibernation, cold wake, Worker redeploy, network interruption, duplicate
publish, invalid signature, replay, oversized payload, malformed frame, and
object recreation.

**Evidence checkpoint — August 31, 2026:** The repeatable
`realtime:load` harness opened 1, 10, 100, and 200 concurrent clients in four
bounded groups against the remote beta Worker (311 connections and four
publications total). Every client received its target revision. The one-run
measurements were:

| Clients | Connect p50 / p95 / max | Delivery p50 / p95 / max | Delivered |
| ------- | ----------------------- | ------------------------ | --------- |
| 1 | 269 / 269 / 269 ms | 69 / 69 / 69 ms | 1 / 1 |
| 10 | 121 / 155 / 155 ms | 57 / 59 / 59 ms | 10 / 10 |
| 100 | 427 / 538 / 548 ms | 120 / 137 / 138 ms | 100 / 100 |
| 200 | 994 / 1,079 / 1,088 ms | 159 / 194 / 198 ms | 200 / 200 |

These figures prove bounded fanout functionality from one test location; they
are not production latency claims. Repeat runs, sustained connections, rapid
updates, hibernation/wake, interruption, and device testing remain open.

**Synthetic A/B checkpoint — September 1, 2026:** The new
`realtime:benchmark` runner compares equal polling and WebSocket cohorts against
the dedicated `william-temple-house-benchmark-e2e` object, using identical
checksummed revisions. Its first local run used 10 clients per cohort, five
publications, and a one-second synthetic polling cadence. All 50 expected
deliveries per cohort converged with no checksum mismatch. Aggregate polling
latency was 499.82 ms p50 and 997.18 ms p95; aggregate realtime latency was
3.62 ms p50 and 5.63 ms p95. The polling cohort made 50 snapshot reads during
the measured changes; the healthy realtime cohort made zero recurring snapshot
reads after its 10 initial connections.

This local result proves the runner and the expected transport distinction. It
does not prove internet latency, Durable Object hibernation, Vercel request
reduction, or Neon compute savings. Those claims require a bounded remote run
with platform metrics and the Phase 3/4 application integration described
below. Full commands, safety caps, report fields, and interpretation live in
`docs/REALTIME_BENCHMARK.md`.

The first remote beta A/B run used the same 10-client/five-publication
configuration. All 50 deliveries per cohort converged; polling measured 504.88
ms p50 and 996.95 ms p95, while realtime measured 73.06 ms p50 and 78.58 ms
p95. A separate realtime-only probe then held five WebSockets open for 60
seconds with no polling or snapshot request to its object. All five clients
received the post-idle revision with 88.16 ms p95 and no checksum mismatch.
This demonstrates request-free idle survival and successful subsequent
delivery. Cloudflare metrics must still establish whether the Durable Object
actually hibernated during that window.

**Legacy proof:** connect an iOS 15.4 simulator and the real iPad mini 4 to the
remote `wss://` development service; leave it idle, background/foreground it,
interrupt Wi-Fi, and confirm recovery on the physical screen.

**Exit gate:** the raw platform meets provisional latency and reconnection
targets, hibernation is visible in metrics, estimated cost is acceptable, and
no client SDK threatens the legacy bundle.

**Rollback:** delete the development Worker/namespace after exporting results;
LOTTO remains untouched.

### Phase 3 — Additive Neon revision and shadow publication

**Objective:** integrate real committed state while no public client trusts the
hub.

**Work:**

- Provision the isolated `beta.williamtemple.app` stack described above before
  using real application integrations.
- Add backward-compatible revision/publication metadata to the database.
- Increment revision and record publication intent inside every state-changing
  transaction.
- Publish the allowlisted projection only after commit.
- Store it in the beta Durable Object namespace.
- Keep all public clients on `/api/state` polling.
- Compare authoritative and hub revision/checksum after every test action.
- Add Admin/internal diagnostics for publication status without changing the
  staff action's success semantics.
- Inject timeouts and failures to select the bounded retry/repair mechanism.

**Required coverage:** every action in the state API, including generate,
append, range changes, serving advance/reverse, Returned/Unclaimed/revert,
reset, restore, undo, redo, hours, rotation, display URL, and announcement.

**Implementation checkpoint — September 1, 2026:** The disabled-by-default
slice is deployed and has passed its first production-shaped beta integration
check. `raffle_state.revision`
and `raffle_public_state_publications` are additive and idempotent. Every
database mutation still converges on the existing `persist()` transaction,
which now allocates the revision and inserts the allowlisted, checksummed
outbox row atomically when enabled. After commit, one request with a 100–5,000
ms bounded timeout may publish to the exact beta Worker host. A hub or evidence
update failure cannot reject the committed staff action. Explicit repair reads
only the newest pending/failed full state, revalidates its strict public schema
and checksum, and reuses its original idempotency identity. Internal status
returns metadata without the payload or secret. Focused tests cover disabled
zero-work behavior, transaction shape, accepted/rejected publication,
evidence-update failure, timeout, redacted network errors, newest-only repair,
and invalid stored evidence. The schema was applied to `neon-copper-queen` and
verified through `information_schema`, `to_regclass`, `pg_indexes`, and an empty
outbox count. A controlled flag-off deployment then advanced revision `1` to
`2` with no outbox row. After installing a rotated beta-only publish secret and
enabling the flag in the isolated beta Vercel environment, another authenticated
write advanced authoritative and publication revision to `3`; the outbox was
accepted after one attempt with no error, and the Durable Object returned the
identical revision and checksum. A new `realtime:shadow:check` command now
parameterizes all 22 persisted mutation variants and combines them with the
protocol, timeout, redaction, diagnostics, and newest-only repair tests. Its
latest run also covers the beta-only diagnostics endpoint and its no-polling
request budget: all 108 focused tests passed, and the full project suite passed
882 tests with one expected production-bundle fixture skipped. This is local
contract evidence, not the Phase 3 exit gate: the production-shaped beta
mutation/fault matrix, application WebSocket observation, and legacy-device
checks remain open.

**Live beta mutation checkpoint — September 1, 2026:** An authenticated run
advanced isolated beta revisions `3` through `14` across generation, serving
next/next/previous, Returned, Unclaimed, status reversion, undo, redo, snapshot
restore, and reset. All 11 resulting outbox rows were accepted on their first
attempt, no row was non-accepted, and every Durable Object revision/checksum
matched Neon at the action checkpoint. Reset returned `/api/state` to an empty
HTTP-200 state, left the hub at matching revision `14`, and produced one
immutable queue-session closeout. Append, extend-range, batch generation, live
configuration changes, application WebSocket observation, and legacy-device
checks remain open.

**Controlled failure/repair checkpoint — September 1, 2026:** A beta-only
publish-token mismatch caused one authenticated display-URL save to commit
Neon revision `15` while the outbox recorded a bounded HTTP 401 failure after
one attempt and the Durable Object remained at revision `14`. After rotating
the matching token into the isolated Vercel beta secret and redeploying the same
commit, the authenticated `/admin/realtime` action retried only that newest
row. It preserved publication ID
`aea72a94-9f3c-410c-ab2d-88cf6c804c84`, changed the outbox to accepted at
attempt count `2`, and converged the Durable Object to revision `15` with
checksum
`sha256:e6a779e36f4a80e1a0a9ea964c3ab1dd0dab7f39afdb8415819cefa87a6d2593`.
The Admin page reported persistence success throughout; production and `main`
were untouched. This closes the live authentication-failure and explicit
newest-only-repair gate.

**Exit gate:** the beta app completes full-stack auth, command, Neon, publish,
WebSocket, CSP, and fallback checks; a representative staging run shows complete
revision agreement;
failed/rolled-back transactions never publish; all injected delivery failures
are visible and repairable; disabling publication leaves current behavior
unchanged.

**Rollback:** disable server publication. Additive database fields remain inert;
the existing state manager and polling clients continue normally.

### Phase 4 — Dual-read internal canary

**Objective:** connect selected clients while polling remains the displayed
source of truth.

**Work:**

- Add a shared realtime client hook behind a runtime cohort flag.
- Run the first browser cohort on `beta.williamtemple.app`; do not connect
  production public clients during the initial comparison.
- Subscribe and record pushed revisions, but continue rendering polled state.
- Compare pushed versus polled revision, payload checksum, and arrival time.
- Exercise Home, Display, Inventory celebration, and Arcade banner consumers.
- Add restrained internal connection diagnostics.
- Verify background-tab behavior and that reconnects do not create overlapping
  sockets or polling loops.

**Implementation checkpoint — September 1, 2026:** The disabled-by-default
Home/Display/Inventory/Arcade slice is deployed only to the isolated beta. It
requires both a beta-only
server flag and the exact `?realtime=observe` browser cohort. One native
WebSocket validates the strict protocol, agency ID, and recomputed checksum;
the observer hashes the already-polled public projection without another
request and reports equality, hub-ahead timing, mismatch, or waiting state.
Polling remains the only rendered authority. Hidden documents close the socket,
visible documents create one replacement, and transport failures use five
bounded delays (1/2/4/8/16 seconds) before stopping. Focused coverage proves
configuration/CSP boundaries, tamper rejection, zero observer fetches,
single-socket reuse, visibility behavior, and the reconnect ceiling. The full
suite, production build, legacy scan, and hydration smoke pass. A control URL
remained
observer-free, while the opt-in Display observer matched stored revision `15`.
A harmless authenticated configuration save then delivered live revision `16`
in `127 ms`, showed the hub ahead, and converged with `/api/state` in `1,221 ms`.
The first-match value stayed fixed across two later refreshes, the socket had
zero reconnects, and `/admin/realtime` reported accepted attempt `1`.
The existing `/api/state` query now returns its authoritative Neon revision in
a response header, and the observer distinguishes exact match, hub-ahead,
poll-ahead, and same-revision checksum mismatch. Inventory reuses its ticket-
celebration poll and Arcade reuses its Now Serving poll, so neither adds another
request loop. Production-shaped exact matches are recorded for all four
surfaces. A 307-second Display observation stayed connected and exact, and the
iOS 15.4 iPad mini 4 simulator returned to the same match after a real app
background/foreground cycle. An isolated network-interruption drill, platform
cost/hibernation window, and physical iPad validation remain open. The browser
run also found an unrelated UTC/Pacific service-date hydration mismatch on both
canary and control pages. The deterministic pantry-timezone repair passed the
same live boundary with no console errors and passed the iOS 15.4 simulator
background/foreground cycle again.

**Exit gate:** pushed state matches authoritative polled state across all
surfaces and wins the latency comparison without increasing errors, memory, or
legacy-device failures.

**Rollback:** turn off client connection for the cohort. No user-visible source
of state has changed.

### Phase 5 — Realtime-source canary with polling safety net

**Objective:** let a small controlled cohort render realtime state while
retaining automatic fallback.

**Implementation status (September 2, 2026):** the beta-only source path is
implemented behind the independent `LOTTO_REALTIME_SOURCE_CANARY=true` switch
and exact `?realtime=source` cohort. Each public surface retains its existing
poller. A same-revision, same-checksum Neon/hub handshake is required before
that poller clears its next timer; later contiguous revisions render directly.
Close, timeout, invalid payload, revision gap/conflict, offline, or foreground
resync revokes source authority and requests an immediate poll. Deterministic
coverage proves the initial request, zero scheduled healthy-source requests,
direct source rendering, and immediate fallback on the shared Display state
owner, plus controller failure and visibility behavior. The source flag is now
deployed only to the isolated beta. Home, Display, Inventory, and Arcade all
handshook at revision `83` and advanced directly to revision `84` after one
harmless authenticated save. A 40-second healthy Display observation produced
zero network requests and zero `/api/state` reads. A tab-local network outage
selected polling fallback with one immediate read; restoration made one
recovery read and returned to exact revision `84`. The iOS 15.4 simulator then
revealed that its initial `navigator.onLine` hint could remain falsely offline.
After the controller was changed to trust actual offline/online events instead,
the simulator reached `live · r84`, survived a Safari background/foreground
cycle, and received revision `85` while live. Provider-level fault drills, ten
representative service days, sustained platform cost evidence, and the physical
iPad gate remain open. See
[`REALTIME_SOURCE_CANARY.md`](./REALTIME_SOURCE_CANARY.md).

**Owner decision (September 2, 2026):** the ten-service-day beta observation,
provider-wide fault injection, and physical-iPad check are deferred at the
current single-pantry scale. The completed automated, browser, network-fallback,
and iOS 15.4 simulator evidence is accepted as sufficient to plan RC.2 and a
controlled production test. The deferred items remain useful stable-release
evidence; they are not missing implementation work in the Phase 5 source path.

**Work:**

- Apply pushed state for designated staff test devices and one display.
- Stop scheduled polling only after connection plus initial-state handshake is
  healthy.
- On close, timeout, invalid payload, gap, or kill switch, fetch immediately and
  resume adaptive polling.
- Reconnect with bounded exponential backoff and jitter; pause connection work
  appropriately while hidden without compromising a fixed visible kiosk.
- Show a clear non-alarming fallback status during tests.
- Run deliberate Cloudflare, Vercel, and Neon fault scenarios.

**Original formal exit gate (deferred by the owner for the RC program):** at least 10
representative service days with no stale-regression incident, all failure
drills recover, staff writes never depend on hub health, and canary clients
generate no healthy-state `/api/state` polling. The scaled validation decision
above replaces this as the prerequisite for the controlled production test;
the original evidence remains a stable-release consideration.

**Rollback:** disable realtime application for the cohort. The hook immediately
returns to the existing polling source.

### Phase 6 — v2.0.0-rc.2 beta-default release candidate

**Objective:** turn the completed opt-in source proof into the production-ready
release candidate while retaining the stable beta origin.

**Release checkpoint (September 3, 2026):** package metadata, technical and
staff-facing release notes now identify `2.0.0-rc.2`. Ordinary URLs select the
source controller, `?realtime=poll` selects the socket-free control, and the
observer/source query values remain available for diagnostics and
compatibility. The master application gate, authenticated Durable Object
drain/resume control, and unified realtime/polling activity clock are
deployed to the isolated beta. All four ordinary URLs connected at revision
`91`; forced polling, observer comparison, live Worker drain/fallback/resume,
and the iOS 15.4 simulator passed. The Vercel application gate is deployed and
code/build-tested but has not been live-toggled. No production resource or
domain has moved.

**Work:**

- Keep release metadata and release notes at `2.0.0-rc.2` without declaring the
  stable `2.0.0` release.
- Make the realtime/fallback controller the default on the ordinary Home,
  Display, Inventory, and Arcade URLs at `beta.williamtemple.app`.
- Add the explicit `?realtime=poll` control path and retain the observer/source
  query values for RC diagnostics and compatibility.
- Replace test-only source presentation with restrained supported
  live/reconnecting/fallback status where it helps the tester; do not alarm
  ordinary visitors during healthy operation.
- Preserve the complete adaptive polling implementation; deploy and remotely
  verify the implemented Vercel application gate and Cloudflare drain/resume
  control.
- Run the existing focused, full-suite, build, legacy-bundle, hydration,
  four-surface, revision, and fallback tests against ordinary beta URLs.
- Prepare the production resource/configuration manifest, Pro-account move,
  generated-hostname smoke procedure, domain cutover, and rollback checklist.

**Exit gate:** ordinary beta URLs use realtime automatically on all four
surfaces, the polling-only override and automatic fallback pass, the production
bundle and iOS 15.4 simulator pass, and the exact RC commit plus production
resource/rollback checklist are ready for an explicit cutover decision.
Application behavior has met this gate on beta. Phase 7 still requires the
separate Pro-account project, production-scoped resources, generated-host
verification, rollback rehearsal, and the owner's explicit cutover decision.

**Rollback:** redeploy the prior beta build or disable default realtime. Beta
Neon remains authoritative and no production resource has changed.

### Phase 7 — Production RC cutover and live validation

**Objective:** test the exact RC architecture at the one active pantry with all
clients and staff using ordinary production URLs.

**Work:**

- Create the successor project in the consulting business's confirmed paid
  Vercel Pro account before public cutover.
- Deploy the exact RC commit with production-scoped resources behind a generated
  Vercel hostname; do not reuse the beta database, auth records, Blob store,
  Durable Object namespace, or secrets.
- Verify production authentication, one harmless staff mutation, revision and
  checksum agreement, all four default-realtime surfaces, forced polling, and
  automatic fallback before assigning the apex domain.
- Coordinate the `williamtemple.app` assignment and authentication callback
  settings without changing application code between the generated-host test
  and the cutover.
- Schedule the coordinated DNS/domain change no earlier than Thursday,
  September 3, 2026 after 2:30 PM America/Los_Angeles, and only with the owner
  present for authentication or provider intervention.
- Monitor Vercel, Cloudflare, Neon, and LOTTO publication diagnostics during
  the first live service day using the lean launch checklist above.
- Roll back immediately on unexplained revision divergence, repeated healthy-
  network fallback, failed newest publication, material Function errors, or an
  unexpected usage surge.
- Retire the beta Hobby project and prior production project only after the
  owner accepts the Pro deployment; preserve both during cutover and the agreed
  rollback checkpoint.

**Exit gate:** the production RC completes its agreed live test with correct
updates and acceptable provider signals. The owner then makes an explicit
stable-release, extend-test, or rollback decision.

**Rollback:** disable realtime to resume polling, or reassign
`williamtemple.app` to the previous known-good Vercel deployment. Neon remains
complete and authoritative.

### Phase 8 — Stable v2.0 promotion and observation

**Objective:** convert an accepted production RC into supported `v2.0.0` while
keeping the early release reversible.

- Finalize protocol, key rotation, agency provisioning, rebuild-from-Neon,
  incident, quota, and rollback runbooks.
- Remove test-only diagnostics while retaining actionable connection and
  publication health.
- Update deployment, security, browser support, polling, user guides,
  environment examples, schema documentation, and final release notes.
- Preserve polling fallback and kill switches through the first stable v2.x
  observation window.
- Review latency, reconnection, Neon compute, Vercel usage, and Cloudflare cost
  after launch and then on the normal operational cadence.
- Do not promote Durable Object storage to queue authority without a separate
  architecture plan and major data-migration review.

## Test and validation matrix

| Area              | Required evidence                                                                        |
| ----------------- | ---------------------------------------------------------------------------------------- |
| Unit              | Projection allowlist, revision ordering, checksum, protocol parsing, backoff bounds      |
| State manager     | Atomic revision + snapshot + outbox for every mutation                                   |
| Contract          | LOTTO publisher and Worker agree on schemas and error semantics                          |
| Durable Object    | Storage, hibernation, reconnect, redeploy, duplicate/out-of-order publish                |
| Client            | Initial handshake, atomic update, gap recovery, fallback, visibility, cleanup            |
| Integration       | Neon commit through broadcast to every public surface                                    |
| Failure injection | Neon rollback, Vercel timeout, Worker error, DO overload, network split, quota rejection |
| Load              | Projected 1×, 10×, and 100× clients and changes; reconnect storm                         |
| Security          | Forged publish, replay, malformed/oversized payload, secret leakage, rate limiting       |
| Cost              | Neon/Vercel/Cloudflare before-and-after metrics with client/action counts                |
| Legacy            | RC.2: production bundle plus remote `wss://` on iOS 15.4 simulator; physical iPad deferred to the stable-release decision |
| Accessibility     | Status announcements do not distract; fallback remains understandable                    |

The RC.2 legacy sequence remains mandatory: build, run
`check:legacy-bundles`, run `smoke:legacy`, deploy to the stable beta origin,
and validate realtime/fallback on the iOS 15.4 simulator. The project owner has
deferred the real-iPad exercise for the production-test decision; record that
exception rather than treating the simulator as hardware evidence. Revisit the
physical device before stable `v2.0.0`. Remove `.next` after production-build
verification as required by `AGENTS.md`.

## v2.0.0-rc.2 production-test gates

RC.2 is ready to test at `williamtemple.app` when all of the following are true:

- package metadata, visible About/version surfaces, release notes, and the Git
  tag all use `2.0.0-rc.2` / `v2.0.0-rc.2` consistently;
- ordinary Home, Display, Inventory, and Arcade URLs select realtime after the
  exact Neon/hub handshake, without requiring a query parameter;
- `?realtime=poll`, the deployment kill switch, and automatic transport/data
  fallback all restore the existing adaptive polling path;
- the full suite, production build, legacy scan, hydration smoke, iOS 15.4
  simulator, four-surface beta update, and fallback checks pass;
- the exact RC commit is deployed behind a generated Vercel hostname with
  production-scoped resources and no beta data or secret reuse;
- the production Vercel project is on Pro or Vercel has confirmed another
  eligible arrangement in writing;
- the prior production deployment and domain reassignment procedure are ready
  for rollback; and
- the Vercel, Cloudflare, Neon, and LOTTO diagnostics named in the lean launch
  checklist are available during the first production service day.

## Stable v2.0 release gates

The provisional decision becomes accepted only when all of the following are
true:

- the production canary completes its full observation window;
- public state reaches stable connected clients within the accepted latency
  objectives;
- Neon and hub revision/checksum comparisons show no unexplained divergence;
- all mutation, undo, restore, and reset paths publish correctly;
- Cloudflare loss never prevents an authoritative staff action;
- automatic fallback succeeds in every supported public surface;
- healthy clients produce no ordinary `/api/state` polling;
- public-origin Neon reads fall by at least 95% versus baseline;
- current and projected Cloudflare cost fit the approved budget;
- hard quota behavior is known and documented;
- security review and secret rotation pass;
- the simulator passes and the deferred real-iPad requirement is either
  completed or explicitly resolved at the stable-release decision;
- deployment, recovery, rebuild, rollback, and incident runbooks are exercised;
- relevant user guides describe any visible connection/fallback behavior; and
- the current polling implementation remains available as supported degraded
  mode.

Failure of a gate does not require forcing the design through. The experiment
may revise the protocol, repeat a phase, adopt a managed realtime provider, fall
back to tagged CDN delivery, or conclude that current polling remains the better
operational tradeoff.

## Documentation deliverables by phase

| Phase | Documentation                                                                  |
| ----- | ------------------------------------------------------------------------------ |
| 0     | Baseline report and accepted service objectives                                |
| 1     | Public-state schema and protocol decision record                               |
| 2     | Cloudflare proof report, cost model, and platform constraints                  |
| 3     | Additive schema, publisher, recovery, and shadow-mode notes                    |
| 4–5   | Client state-machine, fallback, and canary test instructions                   |
| 6     | RC.2 release notes, beta-default behavior, production manifest, and cutover checklist |
| 7     | Production RC test record, provider usage checkpoint, and go/no-go decision    |
| 8     | Deployment, security, browser support, polling, user guides, final release notes, and post-release review |

## Open decisions for the proof program

These are experiments to resolve, not reasons to guess before Phase 0:

1. Whether a direct signed publish plus Neon outbox is sufficient or a
   Cloudflare Queue is justified.
2. Whether protocol code should be a small shared workspace package or separate
   modules governed by generated schema and contract tests.
3. The maximum serialized public-state payload and whether a full state message
   remains economical at 100× load.
4. How quickly a browser should declare a silent connection unhealthy without
   adding wakeful application heartbeats.
5. Whether connection status belongs on all public surfaces or only operational
   Display/Admin diagnostics.
6. Whether one shared multi-agency Worker or per-agency Worker deployments offer
   the better security, provisioning, and cost boundary.
7. The approved paid-plan/fallback behavior when Workers Free limits are
   approached or exceeded.
8. What caused the January 2026 Blob experiment rollback and which lessons apply
   to any derived public-state store.

## Primary references

- [Cloudflare Durable Objects pricing](https://developers.cloudflare.com/durable-objects/platform/pricing/)
- [Cloudflare Durable Objects limits](https://developers.cloudflare.com/durable-objects/platform/limits/)
- [Cloudflare Durable Objects WebSockets and hibernation](https://developers.cloudflare.com/durable-objects/best-practices/websockets/)
- [Cloudflare SQLite-backed transactional storage](https://developers.cloudflare.com/durable-objects/api/sqlite-storage-api/)
- [Cloudflare Durable Objects error handling](https://developers.cloudflare.com/durable-objects/best-practices/error-handling/)
- [Cloudflare Durable Objects development and deployment](https://developers.cloudflare.com/durable-objects/get-started/)
- [Cloudflare Workers observability](https://developers.cloudflare.com/workers/observability/)
- [Cloudflare Worker routes and custom domains](https://developers.cloudflare.com/workers/configuration/routing/)
- [Cloudflare `workers.dev` development hostnames](https://developers.cloudflare.com/workers/configuration/routing/workers-dev/)
- [Neon Scale to Zero](https://neon.com/docs/introduction/scale-to-zero)
- [Neon logical replication and Scale to Zero](https://neon.com/docs/guides/logical-replication-neon)
- [Vercel custom-domain and subdomain setup](https://vercel.com/docs/domains/set-up-custom-domain)
- [Vercel CDN pricing and usage](https://vercel.com/docs/manage-cdn-usage)
- [Vercel Fluid Compute pricing](https://vercel.com/docs/functions/usage-and-pricing)
- [Vercel plan and limit behavior](https://vercel.com/docs/plans)
- [Vercel fair-use and commercial-use guidance](https://vercel.com/docs/limits/fair-use-guidelines)
- [Vercel usage inspection and optimization](https://vercel.com/docs/pricing/manage-and-optimize-usage)
- [Vercel Spend Management](https://vercel.com/docs/spend-management)
- [LOTTO usage-cost analysis](../USAGE_COSTS.md)
- [LOTTO polling strategy](./POLLING.md)

---

**Document version:** 1.16

**Owner:** v2.0 realtime architecture proof program
